{# Phase 0 stub — markup migrates from template-parts/layout/preloader.php in Phase 5. #}
{% set style = style|default(options.preloader_style|default('v1')) %}
<div class="f-preloader" data-preloader-style="{{ style }}" hidden></div>
