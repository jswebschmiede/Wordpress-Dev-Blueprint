/**
 * Initializes the boilerplate plugin frontend entry.
 *
 * @returns {void}
 */
function initBoilerplatePluginFrontend() {
    document.documentElement.classList.add('has-boilerplate-plugin');
}

initBoilerplatePluginFrontend();
