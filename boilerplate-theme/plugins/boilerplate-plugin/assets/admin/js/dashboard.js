/**
 * Initializes the boilerplate plugin admin entry.
 *
 * @returns {void}
 */
function initBoilerplatePluginAdmin() {
    document.documentElement.classList.add('has-boilerplate-plugin-admin');
}

initBoilerplatePluginAdmin();
